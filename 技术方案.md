# 数字人口播视频生成技术方案

## 1. 总体流程

本系统用于将抖音视频链接转换为可二创的数字人口播视频。整体链路如下：

```text
抖音视频链接
  -> 下载视频到本地
  -> ffmpeg 提取音频
  -> qwen3.5-omni-plus 识别原始口播文案
  -> qwen3.5-flash 根据用户要求改写文案
  -> qwen3-tts-flash 合成新语音
  -> wan2.2-s2v 根据人物图片和语音生成口型同步视频
  -> qwen3.5-omni-plus 识别最终视频、生成字幕、检查一致性
  -> qwen-image-2.0-pro 生成封面
  -> 导出最终成品
```

## 2. 技术栈

前端：

```text
Vue 3
Vite
TypeScript
四列流程工作台 UI
```

后端：

```text
Python
FastAPI
ffmpeg
DashScope SDK
本地 storage 文件系统
异步任务 / 前端轮询
```

模型：

```text
第一步：qwen3.5-omni-plus
第二步：qwen3.5-flash
第三步：qwen3-tts-flash
第四步：wan2.2-s2v
第五步：qwen3.5-omni-plus
第六步：qwen-image-2.0-pro
```

## 3. 第一步：抖音视频下载、音频提取、文案识别

输入：

```text
douyin_url
```

实现步骤：

1. 用户在前端输入抖音视频链接。
2. 后端创建项目并保存 `douyin_url`。
3. 后端调用下载模块，将抖音视频下载到本地。
4. 使用 `ffmpeg` 从视频中提取音频。
5. 调用 `qwen3.5-omni-plus` 对音频进行文字识别。
6. 保存识别出的原始口播文案。

本地文件建议：

```text
storage/projects/{project_id}/source.mp4
storage/projects/{project_id}/audio.wav
```

音频提取命令：

```bash
ffmpeg -y -i source.mp4 -vn -acodec pcm_s16le -ar 16000 -ac 1 audio.wav
```

输出字段：

```json
{
  "source_video_path": "storage/projects/{project_id}/source.mp4",
  "audio_path": "storage/projects/{project_id}/audio.wav",
  "transcript": "原始口播文案"
}
```

## 4. 第二步：文案改写

输入：

```text
transcript
rewrite_prompt
```

使用模型：

```text
qwen3.5-flash
```

实现步骤：

1. 读取第一步生成的 `transcript`。
2. 读取用户输入的 `rewrite_prompt`。
3. 调用 `qwen3.5-flash` 进行短视频口播文案改写。
4. 保存改写后的文案。

Prompt 示例：

```text
你是短视频口播文案优化专家。
请基于以下原始文案进行改写，要求：
{用户改写要求}

原始文案：
{transcript}

输出要求：
1. 保持适合口播
2. 不要输出解释
3. 只输出改写后的文案
```

输出字段：

```json
{
  "rewritten_script": "改写后的文案"
}
```

## 5. 第三步：语音合成

输入：

```text
rewritten_script
voice = Cherry
```

使用模型：

```text
qwen3-tts-flash
```

实现步骤：

1. 读取第二步生成的 `rewritten_script`。
2. 调用 DashScope `MultiModalConversation.call`。
3. 使用 `qwen3-tts-flash` 合成语音。
4. 将流式返回的音频内容保存为本地音频文件。

示例代码：

```python
import os
import dashscope

dashscope.base_http_api_url = "https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api/v1"

response = dashscope.MultiModalConversation.call(
    model="qwen3-tts-flash",
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    text=rewritten_script,
    voice="Cherry",
    stream=True,
)
```

输出文件建议：

```text
storage/projects/{project_id}/tts.mp3
```

输出字段：

```json
{
  "audio_path": "storage/projects/{project_id}/tts.mp3"
}
```

## 6. 第四步：数字人口型同步视频生成

输入：

```text
avatar_image
tts_audio
```

使用模型：

```text
wan2.2-s2v
```

实现步骤：

1. 用户上传人物图片。
2. 后端读取第三步生成的语音文件。
3. 将人物图片和语音文件上传到公网可访问的对象存储。
4. 调用 `wan2.2-s2v` 视频生成接口。
5. 轮询异步任务结果。
6. 下载生成的视频到本地。

请求示例：

```bash
curl 'https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api/v1/services/aigc/image2video/video-synthesis/' \
  --header 'X-DashScope-Async: enable' \
  --header "Authorization: Bearer $DASHSCOPE_API_KEY" \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "wan2.2-s2v",
    "input": {
      "image_url": "人物图片公网URL",
      "audio_url": "合成语音公网URL"
    },
    "parameters": {
      "resolution": "480P"
    }
  }'
```

注意事项：

```text
wan2.2-s2v 需要 image_url 和 audio_url 是公网可访问地址。
本地文件不能直接传入，需要先上传到 OSS、COS 或后端静态文件服务。
```

输出字段：

```json
{
  "avatar_image_path": "storage/projects/{project_id}/avatar.png",
  "lip_sync_video_path": "storage/projects/{project_id}/digital_human.mp4"
}
```

## 7. 第五步：最终视频识别、字幕生成、一致性检查

输入：

```text
lip_sync_video_path
rewritten_script
```

使用模型：

```text
qwen3.5-omni-plus
```

用途：

```text
识别最终视频口播
生成字幕段落
检查最终视频口播与第二步改写文案是否一致
```

实现步骤：

1. 将第四步生成的最终视频传给 `qwen3.5-omni-plus`。
2. 同时传入第二步的 `rewritten_script` 作为对照文案。
3. 要求模型输出严格 JSON。
4. 后端解析 JSON，保存字幕分段和质检问题。
5. 如需烧录字幕，使用 `ffmpeg` 生成带字幕版本视频。

建议输出 JSON：

```json
{
  "recognized_text": "最终视频识别出的口播文案",
  "segments": [
    {
      "index": 1,
      "start": "00:00",
      "end": "00:03.20",
      "text": "字幕内容"
    }
  ],
  "issues": [
    {
      "type": "text_mismatch",
      "message": "第 2 句与改写文案不一致"
    }
  ]
}
```

字幕烧录示例：

```bash
ffmpeg -i digital_human.mp4 -vf subtitles=subtitle.srt output_with_subtitles.mp4
```

输出字段：

```json
{
  "segments": [],
  "issues": [],
  "subtitle_path": "storage/projects/{project_id}/subtitle.srt"
}
```

## 8. 第六步：封面生成

输入：

```text
cover_title
视频截图
文案主题
```

使用模型：

```text
qwen-image-2.0-pro
```

实现步骤：

1. 前端输入封面标题或从改写文案中自动生成标题。
2. 后端可从最终视频截取一帧作为参考图。
3. 调用 `qwen-image-2.0-pro` 生成封面。
4. 保存封面图片到本地。

Prompt 示例：

```text
为短视频生成一张吸引点击的封面图。
主题：{cover_title}
要求：适合抖音风格，标题清晰，人物主体明确，画面干净，高质感。
```

示例代码：

```python
import json
import os
import dashscope
from dashscope import MultiModalConversation

dashscope.base_http_api_url = "https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api/v1"

messages = [
    {
        "role": "user",
        "content": [
            {"text": prompt}
        ]
    }
]

response = MultiModalConversation.call(
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    model="qwen-image-2.0-pro",
    messages=messages,
    result_format="message",
    stream=False,
    watermark=False,
    prompt_extend=True,
    negative_prompt="低分辨率，低画质，文字模糊，构图混乱，画面具有AI感。",
    size="2048*2048",
)

if response.status_code == 200:
    print(json.dumps(response, ensure_ascii=False))
else:
    print(response.code, response.message)
```

输出字段：

```json
{
  "cover_path": "storage/projects/{project_id}/cover.png"
}
```

## 9. 第七步：导出成品

输入：

```text
digital_human.mp4
subtitle.srt
cover.png
transcript
rewritten_script
metadata
```

实现步骤：

1. 创建导出目录。
2. 拷贝最终视频、封面图、字幕文件。
3. 保存原始文案和改写文案。
4. 生成项目元数据。
5. 可选：使用 `ffmpeg` 输出带字幕版本视频。
6. 可选：压缩为 zip。

导出目录建议：

```text
exports/{project_id}/
  final_video.mp4
  final_video_with_subtitles.mp4
  cover.png
  subtitle.srt
  transcript.txt
  rewritten_script.txt
  metadata.json
```

输出字段：

```json
{
  "export_path": "exports/{project_id}"
}
```

## 10. 后端接口设计

建议接口：

```text
POST /api/projects
创建项目

GET /api/projects/{id}
查询项目状态

POST /api/projects/{id}/upload
上传源视频，可选

POST /api/projects/{id}/avatar
上传人物图片

POST /api/projects/{id}/steps/extract/run
执行第一步

POST /api/projects/{id}/steps/rewrite/run
执行第二步

POST /api/projects/{id}/steps/tts/run
执行第三步

POST /api/projects/{id}/steps/lipsync/run
执行第四步

POST /api/projects/{id}/steps/subtitles/run
执行第五步

POST /api/projects/{id}/steps/cover/run
执行第六步

POST /api/projects/{id}/steps/export/run
执行第七步
```

## 11. 项目状态结构

建议后端保存统一项目状态：

```json
{
  "id": "project_id",
  "name": "数字人口播项目",
  "status": "queued",
  "douyin_url": "",
  "source_video_path": "",
  "audio_path": "",
  "transcript": "",
  "rewrite_prompt": "",
  "rewritten_script": "",
  "avatar_image_path": "",
  "lip_sync_video_path": "",
  "segments": [],
  "issues": [],
  "cover_path": "",
  "export_path": "",
  "logs": []
}
```

## 12. 关键注意事项

1. 第四步 `wan2.2-s2v` 必须使用公网可访问的 `image_url` 和 `audio_url`。
2. 第三步 TTS 返回是流式结果，后端需要把音频内容保存成文件。
3. 第五步必须要求模型输出严格 JSON，方便前端渲染字幕和质检结果。
4. 每一步执行完成后都要写入项目状态，前端只负责触发和展示。
5. 长耗时任务建议异步执行，前端轮询项目状态。
6. 所有生成文件建议按照 `project_id` 隔离，避免多任务互相覆盖。
7. API Key、WorkspaceId、对象存储密钥必须走环境变量，不要写死在代码里。
